

<h1>Status#summary</h1>
<p>Find me in app/views/status/summary.html.erb</p>

 <button id="renderBtn">
 	Render
</button>

<% @plot_ids.each do |p| %>
<div class="container">
	<h3><%= p.description %></h3>
	<canvas id="<%= p.div_id %>"></canvas>
</div>
<% end %>

<%= @plots.to_json  %>

<script type="text/javascript">
var data = <%= raw @plots.to_json  %>
console.log(data)
function renderChart(data, div_id) {
    var ctx = document.getElementById(div_id).getContext('2d');
    var labels = data["all_months"];
    var datasets =  data["datasets"]; 
    var myChart = new Chart(ctx, {
        type: 'line',
        data: {
            labels: labels,
            datasets: datasets
        },
    });
}


$("#renderBtn").click(
	
    function () {
        //data   = [20000, 14000, 12000, 15000, 18000, 19000, 22000];
        //data2  = [14000, 12000, 15000, 18000, 19000, 22000,20000 ];
        //labels = ["sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "saturday"];
        <% @plot_ids.each do |p| %>
        var tmp_data = data["<%= p.div_id %>"] ;

        renderChart(tmp_data, "<%= p.div_id %>");
        <% end %>
    }
);


</script>