

<h1>Status#summary</h1>
<p>Find me in app/views/status/summary.html.erb</p>

 <button id="renderBtn">
 	Render
</button>

<% @plot_ids.each do |p| %>
<div class="container">
	<h3><%= p.description %></h3>
	<canvas id="<%= p.div_id %>"></canvas>
</div>
<% end %>

<%= @plots.to_json  %>

<script type="text/javascript">
data = <%= raw @plots.to_json  %>
console.log(data)
function renderChart(data, labels, div_id) {
    var ctx = document.getElementById(div_id).getContext('2d');
    var myChart = new Chart(ctx, {
        type: 'line',
        data: {
            labels: labels,
            datasets: [{
                label: 'This week',
                data: data,
                },{
                label: 'next week',
                data: data2,
            }]
        },
    });
}

$("#renderBtn").click(
	
    function () {
        data   = [20000, 14000, 12000, 15000, 18000, 19000, 22000];
        data2  = [14000, 12000, 15000, 18000, 19000, 22000,20000 ];
        labels = ["sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "saturday"];
        renderChart(data,data2, labels);
    }
);


</script>